---
RecipeFormatVersion: "2020-01-25"
ComponentName: "<<COMPONENTFULLNAME>>"
ComponentVersion: "1.0.0"
ComponentDescription: "<<DESCRIPTION>>"
ComponentPublisher: "<<AUTHOR>>"
ComponentDependencies:
  aws.greengrass.TokenExchangeService:
    VersionRequirement: ">=2.0.0"
    DependencyType: "HARD"
ComponentConfiguration:
  DefaultConfiguration:
    ComponentConfig:
      logging:
        level: "INFO"
        python_format: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
      heartbeat:
        enabled: true
        intervalSecs: 5
        # The state keepalive publishes to ecv1/{device}/{component}/main/state; the
        # measures are emitted as the 'sys' metric through metricEmission.
        destination: "local"
        measures: { cpu: true, memory: true, disk: false }
      tags: {}
      metricEmission:
        target: "log"
        targetConfig:
          logFileName: "/greengrass/v2/logs/{ComponentFullName}.metric.log"
      component:
        global:
          defaults:
            pollIntervalMs: 1000
          healthThresholds:
            staleSignalSecs: 30
        instances:
        # Ships against the in-process simulator (adapter: sim) so it runs with no hardware. Point
        # `adapter` at your protocol backend and `connection.endpoint` at the real device, and add
        # any writeable signal ids to `writes.allow` (empty means read-only).
        - id: device1
          adapter: sim
          connection:
            endpoint: sim://device1
          pollIntervalMs: 1000
          writes:
            allow: []
    accessControl:
      aws.greengrass.ipc.pubsub:
        "<<COMPONENTFULLNAME>>:ipc:1":
          policyDescription: Allows publish/subscribe on all IPC topics
          operations: ["aws.greengrass#PublishToTopic", "aws.greengrass#SubscribeToTopic"]
          resources: ["*"]
      aws.greengrass.ipc.mqttproxy:
        "<<COMPONENTFULLNAME>>:iotcore:1":
          policyDescription: Allows publish/subscribe on all IoT Core topics
          operations: ["aws.greengrass#PublishToIoTCore", "aws.greengrass#SubscribeToIoTCore"]
          resources: ["*"]
Manifests:
  - Platform:
      os: all
    Artifacts:
      - URI: "s3://BUCKET_NAME/COMPONENT_NAME/COMPONENT_VERSION/<<BINNAME>>.zip"
        Unarchive: ZIP
    Lifecycle:
      install:
        Script: |-
          set -e
          COMPONENT_DIR="{work:path}"
          python3 -m venv "$COMPONENT_DIR/venv"
          . $COMPONENT_DIR/venv/bin/activate
          pip3 install --upgrade pip
          # requirements.txt carries the edgecommons dependency itself (a git pin for
          # --dep-source registry, an editable sibling install for --dep-source local).
          pip3 install -r {artifacts:decompressedPath}/<<BINNAME>>/requirements.txt
          deactivate
      Run:
        Script: |-
          set -e
          COMPONENT_DIR="{work:path}"
          . $COMPONENT_DIR/venv/bin/activate
          python3 -u {artifacts:decompressedPath}/<<BINNAME>>/main.py --platform GREENGRASS
