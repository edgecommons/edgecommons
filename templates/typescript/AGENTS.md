# <<COMPONENTNAME>> — component notes

EdgeCommons component (TypeScript). Full name `<<COMPONENTFULLNAME>>`. Depends on the
`@edgecommons/edgecommons` TypeScript library. Generated by the `edgecommons` scaffolding CLI from
the `typescript/service` template — this file describes the scaffold **as generated**; keep it in
sync with `src/` as you build the component out, the same way every other component in the
ecosystem does (see the org umbrella `AGENTS.md` for the doc-sync rule).

## What it is

A general-purpose component: the library's standard CLI contract, configuration, logging,
messaging, metrics, and heartbeat, plus a small demonstrated monitoring and command surface (a
metric, a data signal, an event, and a custom command verb) so a freshly generated component has
something to show on an edge-console before real business logic exists. The demo loop and the
service wiring live in `src/runtime.ts` (the thin live-runtime seam); the unit-tested decisions it
drives — the connectivity provider and the command verb — live in `src/app.ts`.
Runs on `HOST` / `GREENGRASS` / `KUBERNETES` via the standard edgecommons platform/transport model
— no platform branching in this component's own code.

## Config location

`component.global` (`publish_interval`) and `component.instances[]` (a single `main` instance) —
see `config.schema.json` and `docs/reference/configuration.md`. The sibling sections (`tags`,
`hierarchy`, `identity`, `messaging`, `logging`, `metricEmission`, `heartbeat`) are standard
edgecommons sections owned by the canonical schema, not redeclared here.

## Validation expectations

- `npm run build` (tsc) and `npm test` (vitest) must stay green; `npm run coverage` enforces the
  org's 90%-line gate (`vitest.config.ts` thresholds).
- The demo metric/signal/event/command exist to be replaced, not preserved — once real business
  logic exists, delete them along with the doc pages that describe them (or rewrite the doc pages
  to describe your replacement).

## Org conventions this scaffold inherits

Read the umbrella `../AGENTS.md` (or, outside the monorepo, `core/AGENTS.md`) for the full
design-fidelity contract, the platform/transport model, the UNS grammar, and the validation matrix.
In short: kebab crate/bin/artifact naming, the `data()`/`events()`/`commands()` facades instead of
hand-built topics, and no direct publish to the reserved `state`/`metric`/`cfg`/`log` classes.
