{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://docs.edgecommons.mbreissi.com/schemas/<<COMPONENTNAME>>/config.schema.json",
  "title": "<<COMPONENTNAME>> component configuration",
  "description": "The configuration <<COMPONENTNAME>> itself understands: the object at `component.global` in an EdgeCommons config file. Each entry of `component.instances[]` is validated against `#/$defs/instance`. The library envelope (logging, messaging, metricEmission, heartbeat, streaming, credentials, parameters, health, hierarchy, identity, topic, tags) is owned by the canonical schema (schema/edgecommons-config-schema.json) and is deliberately NOT redeclared here. This schema starts from the scaffold's own example config (test-configs/config.json) — extend it as you add configuration to your component, and keep `additionalProperties: false` so a typo'd or unknown key is caught at deploy time instead of being silently ignored.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "publish_interval": {
      "type": "integer",
      "minimum": 1,
      "default": 3,
      "description": "Seconds between the scaffold's publish tick (the app-status / metric / data / event quartet it emits each loop)."
    }
  },
  "$defs": {
    "instance": {
      "title": "<<COMPONENTNAME>> instance configuration",
      "description": "One entry of `component.instances[]`. The scaffold declares a single instance, `main`, which is the instance its data()/events()/metrics() facades are bound to. Declare the per-instance keys your component reads here as you add them.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "default": "main",
          "description": "Unique instance identifier. It is the `{instance}` token of this instance's UNS topics (ecv1/{device}/{component}/{instance}/...) and is stamped into the envelope identity."
        },
        "publish_interval": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-instance override of the global publish tick, in seconds."
        }
      }
    }
  }
}
