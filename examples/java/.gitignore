*build/
build
.idea/
**.DS_STORE
**.DS_Store
*.iml
target/
greengrass-build/
dependency-reduced-pom.xml

# Local credentials and runtime artifacts — never commit these
creds/
logs/
*.pem
*.key
*.crt
.vault/
# Per-instance local store dirs created by STANDALONE runs (e.g. <thing>-tcplocalhost1883/)
*-tcplocalhost1883/