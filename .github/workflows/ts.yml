name: ts
on:
  push:
    paths: ["libs/ts/**", ".github/workflows/ts.yml"]
  pull_request:
    paths: ["libs/ts/**", ".github/workflows/ts.yml"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: libs/ts
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci || npm install
      - run: npm run build
      - run: npm test
