---
RecipeFormatVersion: "2020-01-25"
ComponentName: "com.ggcommons.TsConfigProvider"
ComponentVersion: "1.0.0"
ComponentDescription: "Config-manager peer for validating the ggcommons TypeScript CONFIG_COMPONENT source on a live nucleus (answers GetConfiguration over IPC)."
ComponentPublisher: "ggcommons"
ComponentConfiguration:
  DefaultConfiguration:
    accessControl:
      aws.greengrass.ipc.pubsub:
        "com.ggcommons.TsConfigProvider:pubsub:1":
          policyDescription: "Allow pub/sub on all local IPC topics."
          operations:
            - "aws.greengrass#PublishToTopic"
            - "aws.greengrass#SubscribeToTopic"
          resources:
            - "*"
Manifests:
  - Platform:
      os: linux
    Artifacts:
      - URI: "s3://BUCKET/com.ggcommons.TsConfigProvider/1.0.0/tsverify.zip"
        Unarchive: ZIP
    Lifecycle:
      Run:
        RequiresPrivilege: true
        Script: "node {artifacts:decompressedPath}/tsverify/dist/config_provider.js com.ggcommons.TsGgVerify"
