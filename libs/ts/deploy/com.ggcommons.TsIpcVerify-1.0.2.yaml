---
RecipeFormatVersion: "2020-01-25"
ComponentName: "com.ggcommons.TsIpcVerify"
ComponentVersion: "1.0.2"
ComponentDescription: "On-device GREENGRASS-mode IPC verification for the ggcommons TypeScript library (cross-language heartbeat ingest + request/reply + raw)."
ComponentPublisher: "ggcommons"
ComponentConfiguration:
  DefaultConfiguration:
    accessControl:
      aws.greengrass.ipc.pubsub:
        "com.ggcommons.TsIpcVerify:ipc:1":
          policyDescription: "Allow pub/sub on all IPC topics for the interop verification."
          operations:
            - "aws.greengrass#PublishToTopic"
            - "aws.greengrass#SubscribeToTopic"
          resources:
            - "*"
Manifests:
  - Platform:
      os: linux
    Artifacts:
      - URI: "s3://BUCKET/com.ggcommons.TsIpcVerify/1.0.2/tsverify.zip"
        Unarchive: ZIP
    Lifecycle:
      Run:
        RequiresPrivilege: true
        Script: "node {artifacts:decompressedPath}/tsverify/dist/ipc_verify.js"
