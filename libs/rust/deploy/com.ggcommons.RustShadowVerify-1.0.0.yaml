---
RecipeFormatVersion: "2020-01-25"
ComponentName: "com.ggcommons.RustShadowVerify"
ComponentVersion: "1.0.0"
ComponentDescription: "On-device verification (Rust) of the sanitized default SHADOW name: -c SHADOW with no name -> shadow 'com_ggcommons_RustShadowVerify'."
ComponentPublisher: "ggcommons"
ComponentDependencies:
  aws.greengrass.TokenExchangeService:
    VersionRequirement: ">=2.0.0"
    DependencyType: "HARD"
  aws.greengrass.ShadowManager:
    VersionRequirement: ">=2.0.0"
    DependencyType: "HARD"
ComponentConfiguration:
  DefaultConfiguration:
    accessControl:
      aws.greengrass.ipc.pubsub:
        "com.ggcommons.RustShadowVerify:pubsub:1":
          policyDescription: "Allow pub/sub on all local IPC topics (incl. shadow event topics)."
          operations:
            - "aws.greengrass#PublishToTopic"
            - "aws.greengrass#SubscribeToTopic"
          resources:
            - "*"
      aws.greengrass.ShadowManager:
        "com.ggcommons.RustShadowVerify:shadow:1":
          policyDescription: "Allow get/update of thing shadows."
          operations:
            - "aws.greengrass#GetThingShadow"
            - "aws.greengrass#UpdateThingShadow"
            - "aws.greengrass#DeleteThingShadow"
          resources:
            - "*"
Manifests:
  - Platform:
      os: linux
    Artifacts:
      - URI: "s3://BUCKET/com.ggcommons.RustShadowVerify/1.0.0/shadow_verify"
    Lifecycle:
      Install:
        Script: "chmod +x {artifacts:path}/shadow_verify"
      Run:
        RequiresPrivilege: true
        Script: "{artifacts:path}/shadow_verify -c SHADOW"
