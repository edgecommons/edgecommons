---
title: Heartbeat
description: The library-owned state keepalive — a UNS liveness beacon published every 5 seconds by default, plus system measures emitted as the `sys` metric — all from config.
sidebar:
  order: 5
---

import { Tabs, TabItem, Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

The **heartbeat** subsystem is your component's liveness beacon on the
[Unified Namespace](/guides/unified-namespace/). On every tick it does two things, with **zero
component code**:

1. Publishes a **`state` keepalive** to the component's UNS state topic
   `ecv1/{device}/{component}/main/state` — an envelope named `state` with body
   `{"status": "RUNNING", "uptimeSecs": <n>}`. This *is* the liveness signal a console, historian,
   or watchdog subscribes to (`ecv1/+/+/+/state`).
2. Emits the enabled **system measures** (CPU, memory, disk, threads, open files, file descriptors)
   as a single metric named **`sys`** through the normal [metrics](/guides/metrics/) subsystem — so
   the measures inherit the metric subsystem's full sink routing (log file, messaging, CloudWatch,
   Prometheus).

On graceful shutdown the library additionally publishes a best-effort `{"status": "STOPPED"}` state
(at most once, without `uptimeSecs`), so consumers can distinguish a clean stop from a silent
disappearance.

The heartbeat is **on by default** — every component announces itself every **5 seconds** in all
four languages the moment it starts. You never call a heartbeat API; you shape it with the
`heartbeat` config section.

<Aside type="note" title="Heartbeat is autonomous and library-owned">
No SDK exposes a public accessor to the heartbeat object — there is **no** `getHeartbeat()`,
`heartbeat()`, or equivalent in any of the four languages. The runtime constructs the heartbeat
during initialization and tears it down on close. The `state` class is a **reserved** UNS class:
the heartbeat publishes through the library's privileged internal path, and your own code cannot
publish raw messages to a `state` topic (the
[reserved-class guard](/reference/uns/#the-reserved-class-publish-guard) rejects it).
</Aside>

## The state keepalive

Each tick publishes one envelope to `ecv1/{device}/{component}/main/state`:

```json
{
  "header":   { "name": "state", "version": "1.0", "timestamp": "…", "uuid": "…" },
  "identity": {
    "hier":      [ { "level": "device", "value": "gw-01" } ],
    "path":      "gw-01",
    "component": "my-component",
    "instance":  "main"
  },
  "body": { "status": "RUNNING", "uptimeSecs": 125 }
}
```

- The message name and version are `state` / `1.0` in **all four languages** (the old per-language
  heartbeat message-name divergence is gone).
- `uptimeSecs` is the seconds since component start (monotonic clock). The shutdown `STOPPED` state
  omits it.
- **`heartbeat.destination`** (`local` | `iotcore`, default `local`) selects where the keepalive
  goes: the local/IPC transport, or AWS IoT Core. It governs the **keepalive only** — the measures
  route through the metric subsystem's own target (`metricEmission`).

Watch a device's components come alive from any MQTT client:

<Tabs syncKey="os">
<TabItem label="Linux">

```bash
# Bring up the local broker (EMQX), then subscribe to all state keepalives.
docker compose -f test-infra/compose.yaml up -d
mosquitto_sub -h localhost -p 1883 -t 'ecv1/+/+/+/state' -v
```

</TabItem>
<TabItem label="Windows">

```powershell
# Bring up the local broker (EMQX), then subscribe to all state keepalives.
docker compose -f test-infra/compose.yaml up -d
mosquitto_sub -h localhost -p 1883 -t 'ecv1/+/+/+/state' -v
```

</TabItem>
</Tabs>

## The `sys` measures metric

When measures are enabled, each tick also emits a metric named **`sys`** through the metric
subsystem, carrying one value per enabled measure (flattened: `cpu_usage`, `memory_usage`,
`disk_total`/`disk_used`/`disk_free`, `threads`, `files`, `fds`). Where it lands is decided by your
[`metricEmission` config](/guides/metrics/) — a local metric log by default, or messaging (the UNS
`metric` class), CloudWatch, or Prometheus. The metric's `storageResolution` is `1` when the
interval is under 60 seconds, otherwise `60`.

### What it measures

Counter availability varies by language and platform. Where a counter is enabled but unavailable on
the current platform, the value falls back as noted.

| Measure | Unit | Availability and fallbacks |
|---------|------|----------------------------|
| `cpu` | Percent | All languages, all platforms. |
| `memory` | MB (resident set) | All languages, all platforms. Java reports whole-number MB (integer division); Python, Rust, and TypeScript report fractional MB. |
| `disk` | Gigabytes (`disk_total`, `disk_used`, `disk_free`; `disk_used = disk_total − disk_free`) | All languages, all platforms. Java and Python measure the filesystem holding the **parent** of the working directory (`..`); Rust and TypeScript measure the current working directory's filesystem. (Usually the same mount, so the numbers match.) |
| `threads` | Count | Java and Python: all platforms. Rust: Linux and Windows. TypeScript: **Linux only** (returns `0` elsewhere). |
| `files` | Count (open files) | Java and Python: all platforms. Rust: Linux (regular-file descriptors) and Windows (total handles). TypeScript: **Linux only** (returns `0` elsewhere). |
| `fds` | Count (open file descriptors) | Java: Unix only (**`-1` on Windows**). Python: all platforms (Windows reports handle count). Rust: Linux and Windows. TypeScript: **Linux only** (returns `0` elsewhere). |

<Aside type="caution" title="Platform-specific counters">
`threads`, `files`, and `fds` are the least portable. If you depend on them, confirm they are
available on your target platform — on TypeScript they are Linux-only, and Java's `fds` is `-1` on
Windows. CPU, memory, and disk are safe everywhere.
</Aside>

## Configuration

The `heartbeat` section has four keys — and the defaults are **identical in all four languages**,
so an absent section gives you a working keepalive out of the box:

```json
{
  "heartbeat": {
    "enabled": true,
    "intervalSecs": 5,
    "measures": {
      "cpu": true,
      "memory": true,
      "disk": false,
      "threads": false,
      "files": false,
      "fds": false
    },
    "destination": "local"
  }
}
```

| Field | Type | Default | Meaning |
|-------|------|---------|---------|
| `enabled` | boolean | `true` | Whether the heartbeat (state keepalive + `sys` measures metric) runs. |
| `intervalSecs` | integer (min 1) | `5` | Tick period in seconds. |
| `measures.*` | boolean | `cpu`/`memory` on, rest off | Per-measure toggles for the `sys` metric. |
| `destination` | string | `local` | `local` or `iotcore` — where the **state keepalive** publishes. The measures route through `metricEmission`. |

<Aside type="danger" title="heartbeat.targets[] is removed">
The old `heartbeat.targets[]` array (with per-target `type`/`config.destination`/`config.topic`) no
longer exists, and the heartbeat topic is **no longer configurable** — it is always the component's
UNS state topic. A config that still carries `targets` **fails schema validation at startup** with
a precise error. Migrate: delete `targets`, keep `intervalSecs`/`measures`, add `destination` if you
routed the old messaging target to IoT Core, and route the measures via
[`metricEmission`](/guides/metrics/).
</Aside>

<Aside type="tip" title="Customizing is config, not code">
Tuning the interval, choosing measures, disabling the heartbeat, and selecting the keepalive
destination are all done in the `heartbeat` config section — identically across Java, Python, Rust,
and TypeScript. Changes are picked up without a restart: `enabled`, `measures`, and `destination`
apply on the next tick, and an `intervalSecs` change rebuilds the schedule (Java and Python react
via the configuration-change listener; Rust and TypeScript re-read the live config each tick).
</Aside>

## Behavior notes

- **First tick** fires at roughly `t = 0` (component start), then every `intervalSecs`.
- **Every tick is exception-guarded**, and the state and metric halves are individually best-effort:
  a failure in one is logged and does not suppress the other, and the next tick still fires.
- **No messaging transport** (a metrics-only run) skips the keepalive; the `sys` metric still flows
  through the metric subsystem.
- **The `STOPPED` state is best-effort** — published at most once on graceful shutdown
  (`SIGTERM`/`close`); a hard kill obviously cannot publish it. For faster crash detection on MQTT,
  a component may also register an [LWT](/guides/messaging/#mqtt-last-will-lwt).
- Liveness consumers should treat a missed keepalive window (e.g. 2–3 intervals) as *stale*, and the
  `STOPPED` state or an LWT as authoritative offline signals.

## Related

<CardGrid>
  <LinkCard title="Unified Namespace" href="/guides/unified-namespace/" description="The state class, the topic grammar, and the six-wildcard consumer pattern." />
  <LinkCard title="API reference: Heartbeat" href="/reference/heartbeat/" description="The heartbeat types, lifecycle, HeartbeatMonitor, and the config model." />
  <LinkCard title="Metrics guide" href="/guides/metrics/" description="Where the sys measures metric goes — targets and routing." />
  <LinkCard title="Configuration guide" href="/guides/configuration/" description="Config sources and the schema that validates the heartbeat section." />
</CardGrid>
